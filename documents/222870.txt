Corel draw 13 mysql query
mysql coreldraw
Is there a way to do a mysql database query in Corel Draw 13(X3)? 


If I recall correctly, CorelDraw uses VBA as scripting language, no?
If so, if it is not restricted, you can probably use ODBC to do that. See MySQL Connector/ODBC for details.

To follow up PhiLho's point, here's a Google-scanned book that shows using ODBC to connect to an MS-Access database
