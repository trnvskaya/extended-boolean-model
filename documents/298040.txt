Which command will executed first,If a stored procedure have individual multiple select commands;
sql-server-2005
Which command will executed first,If a stored procedure have individual multiple select commands;


Commands inside stored procedure are executed in the order in which they are written.
